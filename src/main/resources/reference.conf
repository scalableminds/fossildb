//default configuration. Overwrite with yours in new file application.conf (create in same directory)

fossildb {
  dataDir = "data"
  backupDir = "backup"
  columnFamilies = ["collection1", "collection2"]
  port = 8090
}